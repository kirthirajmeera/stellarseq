# Model Files Required

Copy these files from your `stellarseq/models/` directory:

1. best_model.pkl
2. scaler.pkl
3. training_metadata.json (optional)

Command:
```bash
cp ../models/best_model.pkl .
cp ../models/scaler.pkl .
cp ../models/training_metadata.json .
```
